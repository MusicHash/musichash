let moduleName = 'app.factories';

import angular from 'angular';
import Factory from '../utils/factory';

angular.module(moduleName, [])
    //.factory('xhr', Factory(xhrFactory))
    ;

export default moduleName;